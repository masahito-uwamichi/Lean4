
\section{数学的証明のサンプル}

このファイルには、PDF出力用の数学的証明の例が含まれています。
基本的な代数と解析の定理を証明します。

\subsection{目次}
1. 自然数の基本性質
2. 実数の性質
3. 指数関数の性質
4. 組み合わせ論的証明



For n , we have:
\[ \mathbb{N}) : n + 0 = n \]
\begin{proof}
Using: \texttt{rw [Nat.add\_zero]}
\end{proof}
\end{theorem}


\begin{theorem}[定理 1.2]
For n , we have:
\[ \mathbb{N}) : 0 + n = n \]
\begin{proof}
Using: \texttt{rw [Nat.zero\_add]}
\end{proof}
\end{theorem}


\begin{theorem}[定理 1.3]
For m n , we have:
\[ \mathbb{N}) : m + n = n + m \]
\begin{proof}
Using: \texttt{rw [Nat.add\_comm]}
\end{proof}
\end{theorem}


\begin{theorem}[定理 1.4]
For l m n , we have:
\[ \mathbb{N}) : (l + m) + n = l + (m + n) \]
\begin{proof}
Using: \texttt{rw [Nat.add\_assoc]}
\end{proof}
\end{theorem}



For x , we have:
\[ \mathbb{R}) : x + 0 = x \]
\begin{proof}
Using: \texttt{rw [add\_zero]}
\end{proof}
\end{theorem}


\begin{theorem}[定理 2.2]
For x , we have:
\[ \mathbb{R}) : x * 1 = x \]
\begin{proof}
Using: \texttt{rw [mul\_one]}
\end{proof}
\end{theorem}


\begin{theorem}[定理 2.3]
For a b c , we have:
\[ \mathbb{R}) : a * (b + c) = a * b + a * c \]
\begin{proof}
Using: \texttt{rw [mul\_add]}
\end{proof}
\end{theorem}


\begin{theorem}[定理 2.4]
For x , we have:
\[ \mathbb{R}) : 0 \leq x\textasciicircum{}2 \]
\begin{proof}
Using: \texttt{exact sq\_nonneg x}
\end{proof}
\end{theorem}



For x y , we have:
\[ \mathbb{R}) : |x + y| \leq |x| + |y| \]
\begin{proof}
Using: \texttt{exact abs\_add x y}
\end{proof}
\end{theorem}


\begin{theorem}[定理 3.2]
For x y , we have:
\[ \mathbb{R}) (hx : 0 \leq x) (hy : 0 \leq y) : \]
\begin{proof}
Using: \texttt{sorry -- この証明は複雑なため省略}
\end{proof}
\end{theorem}



For n , we have:
\[ \mathbb{N}) (h : n > 0) : \]


\begin{definition}
\texttt{fib : \mathbb{N} \to \mathbb{N}}
\end{definition}


\texttt{fib\_zero : fib 0 = 0 := rfl}
\texttt{fib\_one : fib 1 = 1 := rfl}
For n , we have:
\[ \mathbb{N}) : fib (n + 2) = fib n + fib (n + 1) := rfl \]

\begin{theorem}[定理 5.2]
For n , we have:
\[ \mathbb{N}) (h : n > 0) : fib n > 0 \]
\begin{proof}
Using: \texttt{cases n with}
\end{proof}
\end{theorem}

\begin{proof}
Using: \texttt{cases m with}
\end{proof}
\end{theorem}

\begin{proof}
Using: \texttt{cases k with}
\end{proof}
\end{theorem}

\begin{proof}
Using: \texttt{rw [fib\_succ\_succ]}
\end{proof}
\end{theorem}

\begin{proof}
Using: \texttt{exact Nat.add\_pos\_of\_pos\_of\_nonneg h1 h2}
\end{proof}
\end{theorem}



For n , we have:
\[ \mathbb{N}) (x : \mathbb{R}) (h : n > 0) : \]
\begin{proof}
Using: \texttt{constructor}
\end{proof}
\end{theorem}


